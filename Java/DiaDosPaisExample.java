import java.util.ArrayList;
import java.util.List;

public class DiaDosPaisExample {
    public static void main(String[] args) {
        // Creating an empty list and adding elements to it
        List<String> Dia_dos_pais = new ArrayList<>();
        
        // Adding "negueba" to the list
        Dia_dos_pais.add("negueba");

        // Checking if "negueba" is in the list
        if (Dia_dos_pais.contains("negueba")) {
            // Print statement since the main method cannot return a value
            System.out.println("Material de estudo Java");
        }
    }
}
